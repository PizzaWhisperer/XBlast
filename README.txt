The class Board has been modified. We add to it a static method which allows to create a default board. 

The class Player has been modified, we added to it a static method allowing to create a list of four players.

The class Level has been modified. We imbricated a public static class into it. This class represents a specific level with player placed at the 
four edges of a walled board (this case are supposed to be FREE)

